version: 1
name: Java with Maven
stages:
  - name: Build and test Java app
    type: ci
    spec:
      clone:
        disabled: false
      steps:
        - name: Build
          type: script
          spec:
            run: |-
              echo "Hello world"